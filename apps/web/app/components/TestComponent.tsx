// I was trying to test eslint enforcement by purposely making this test component with TS/ES6 violations
// I cannot get eslint config to apply to these files idk what I'm doing wrong.
// continuing dev for now and trying to follow convention, will follow up with mo to get enforcement working again.
function TestComponent(){
  return (
    <div>
      
    </div>
  );
};

module.exports = TestComponent;